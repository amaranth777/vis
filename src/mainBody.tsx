import React from 'react';
import Radar from "./Radar";
import Papa from "papaparse";

interface mainBody {
	PokemonId: number;
}

// eslint-disable-next-line @typescript-eslint/no-redeclare
const mainBody: React.FC<mainBody> = (PokemonId) => {

	return (
		<>
			<div>

			</div>
		</>
	);
};


export default mainBody;