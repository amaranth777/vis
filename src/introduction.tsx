import React from 'react';
import Radar from "./Radar";
import Papa from "papaparse";

interface introductionProps {
	PokemonId: number;
}

const introduction: React.FC<introductionProps> = (PokemonId) => {

	return (
		<div>

		</div>
	);
};


export default introduction;